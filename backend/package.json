{
  "name": "recruit-system-backend",
  "version": "1.0.0",
  "private": true,
  "type": "commonjs",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "migrate": "node -e \"require('fs').readFile('./schema.sql','utf8',(e,sql)=>{if(e)throw e;require('./db').query(sql).then(()=>{console.log('마이그레이션 완료');process.exit(0)}).catch(err=>{console.error(err);process.exit(1)})})\""
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.19.2",
    "pg": "^8.12.0"
  }
}
